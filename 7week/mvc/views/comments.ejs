<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>comments</title>
  </head>
  <body>
    <h1>댓글 모음</h1>
    <a href="/">홈으로 이동하기</a>
    <ul>
      <% for (let i = 0; i < commentData.length; i++) { %>
      <li>
        <b><%= commentData[i].userid %></b>
        <a><%= commentData[i].comment %></a>
        <a><%= commentData[i].date %></a>
      </li>
      <% } %>
    </ul>
  </body>
</html>
