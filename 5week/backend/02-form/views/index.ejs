<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ejs</title>
  </head>
  <body>
    <h1>로그인</h1>
    <form action="/get" method="get" target="_blank" name="login">
      <fieldset>
        <legend>로그인 정보</legend>
        <input type="text" name="name" />
        <input type="password" name="pw" />
      </fieldset>

      <input type="radio" name="gender" value="man" id="man" /><label for="man"
        >남자</label
      >
      <input type="radio" name="gender" value="woman" id="woman" /><label
        for="woman"
        >여자</label
      >
      <button type="submit">로그인</button>
    </form>
    <h1>회원 가입 post 요청</h1>
    <form action="/post/ver2" method="post" name="account">
      <input type="text" name="id" pattern="" />
      <input type="password" name="pw" oninput="validCheckPw(this)" />
      <input type="text" name="name" />
      <input type="text" name="email" />
      <button type="submit">회원가입</button>
    </form>
    <script>
      function validCheckPw(el) {
        const regexPw = /^([a-z0-9]){8,12}$/;
        const value = el.value;
        if (regexPw.test(value)) {
          console.log("합격");
        } else {
          console.log("불합격");
        }
      }

      // function validCheckPw(el) {
      //   const regexPw = /^([a-z0-9]){8,12}$/;
      //   const value = el.value;
      //   const error = document.getElementById("error-pw");
      //   // regexPw.test("검사하고 싶은 값") => true or false return
      //   if (regexPw.test(value)) {
      //     console.log("정규식 일치");
      //   } else {
      //     console.log("정규식 불일치");
      //   }
      // }
    </script>
  </body>
</html>
